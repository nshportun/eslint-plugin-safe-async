# npm configuration for security and consistency

# Use exact versions from lockfile
legacy-peer-deps=false

# Require signatures for publishing
sign-git-commit=false
sign-git-tag=false

# Audit settings
audit=true
audit-level=moderate
